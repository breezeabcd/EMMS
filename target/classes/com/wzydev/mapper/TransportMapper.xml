<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wzydev.mapper.TransportMapper">
    <update id="update">
        update transport
        <set>
            <if test="supplyId != null">
                supply_id = #{supplyId},
            </if>
            <if test="destination != null and destination != ''">
                destination = #{destination},
            </if>
            <if test="quantity != null">
                quantity = #{quantity},
            </if>
            <if test="detail != null and detail != ''">
                photo = #{photo},
            </if>
        </set> where id = #{id}
    </update>

    <delete id="deleteByIds">
        delete from transport where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="list" resultType="com.wzydev.pojo.Transport">
        SELECT
        t.*,
        s.name AS supplyName
        FROM
        transport t
        LEFT JOIN
        supply_info s ON t.supply_id = s.id
        <where>
            <if test="supplyName != null and supplyName != ''">
                and s.name like concat('%', #{supplyName}, '%')
            </if>
            <if test="destination != null and destination != ''">
                and t.destination like concat('%', #{destination}, '%')
            </if>
        </where>
        ORDER BY t.update_time DESC
    </select>

    <select id="getById" resultType="com.wzydev.pojo.Transport">
        SELECT
        t.*,
        s.name AS supplyName
        FROM
        transport t
        LEFT JOIN
        supply_info s ON t.supply_id = s.id
        where t.id = #{id}
    </select>
</mapper>